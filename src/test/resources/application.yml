spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mathshare
    username: postgres
    password: mysecretpassword
    driverClassName: org.postgresql.Driver

  jpa:
    properties:
      hiberrnate.jdbc.lob.non_contextual_creation: true

server:
  port: 8080

logging:
  level:
    org.hibernate: ERROR

  path: logs

cross-origin:
  allowed-origins: ${CROSS_ORIGIN_ALLOWED_ORIGINS:http://localhost:3000}
